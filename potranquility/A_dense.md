# A dense







## On NPC Spawn

local xloc = e.self:GetX();

local yloc = e.self:GetY();

local zloc = e.self:GetZ();

eq.set_proximity(xloc - 45, xloc + 45, yloc - 45, yloc + 45, zloc - 3, zloc + 6);
function event_enter(e)

>*A dense fog of incense smoke envelopes you as you enter the dark rooms. The low murmur of meditative mantras reverberates throughout the building. The elders are all in a meditative state.*

**A dense despawns.**